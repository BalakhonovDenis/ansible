- name: Create Docker network
  command: docker stop nginx apaches && docker container prune --force && docker network rm my_network && docker network create my_network

  #docker_network:
  #  name: my_network
  #  state: present
